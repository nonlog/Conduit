<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- System Framework hosts ClipboardService. Conduit's own process is scoped only so the
         module can expose a zero-cost activation marker; no app API is otherwise hooked there. -->
    <string-array name="xposed_scope">
        <item>android</item>
        <item>com.conduit.sync</item>
    </string-array>
</resources>
