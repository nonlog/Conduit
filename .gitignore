# Rust
target/
**/*.rs.bk
*.pdb

# Windows WPF build output
windows/conduit-ui/bin/
windows/conduit-ui/obj/

# Android / Gradle
.gradle/
build/
local.properties
captures/
*.iml
*.apk
*.aab
*.dex

# Captured build output
build-log.txt
build-err.txt
artifacts/

# Signing keys and device identity — must never be committed
*.jks
*.keystore
keystore.properties
identity.bin
noise_static.bin
trusted_peers.json
conduit.toml

# Screenshots and crops pulled off the phone while testing. Nothing at the repo root is
# an image the build wants — app resources live under android/app/src/main/res.
/*.png

# Editors
.idea/
.vscode/
*.swp
*~

# OS
Thumbs.db
desktop.ini
.DS_Store
.reference/
.tools/
.work/
artifacts_download/
